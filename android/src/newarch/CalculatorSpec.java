package com.calculator;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class CalculatorSpec extends NativeCalculatorSpec {
  CalculatorSpec(ReactApplicationContext context) {
    super(context);
  }
}
